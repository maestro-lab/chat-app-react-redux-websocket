# Chat Application - React + Redux + Redux-Saga + WebSocket

## Стек
- **Frontend:** React 18, Redux Toolkit, Redux-Saga, Socket.IO, Vite
- **Backend:** Node.js, Express, Socket.IO, SQLite, JWT

## Запуск

```bash
# Сервер
cd server
npm install
npm start

# Клиент
cd client
npm install
npm run dev